'use client';
import Image from 'next/image'; import { useState } from 'react'; import { X } from 'lucide-react'; import { works } from '@/data/site';
export function Gallery(){const [img,setImg]=useState<string|null>(null); return <><div className="grid auto-rows-[220px] grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-4">{works.map((w,i)=><button key={w.src} onClick={()=>setImg(w.src)} className={`${i===0?'lg:col-span-2 lg:row-span-2':''} ${i===3?'lg:col-span-2':''} group relative overflow-hidden rounded-[2rem] bg-gray-200 focus-ring`}><Image src={w.src} alt={w.category} fill className="object-cover grayscale transition duration-500 group-hover:scale-105"/><span className="absolute left-5 top-5 rounded-full bg-white px-4 py-2 text-sm font-semibold">{w.category}</span></button>)}</div>{img&&<div className="fixed inset-0 z-[80] grid place-items-center bg-black/80 p-4" onClick={()=>setImg(null)}><button className="absolute right-6 top-6 text-white"><X/></button><div className="relative h-[80vh] w-full max-w-5xl"><Image src={img} alt="Работа AVBOOK" fill className="object-contain"/></div></div>}</>}
