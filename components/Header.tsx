'use client';
import { useEffect, useState } from 'react';
import { Menu, X } from 'lucide-react';
import { nav } from '@/data/site';
export function Header(){const [small,setSmall]=useState(false); const [open,setOpen]=useState(false); useEffect(()=>{const on=()=>setSmall(scrollY>24); on(); addEventListener('scroll',on); return()=>removeEventListener('scroll',on)},[]); return <header className={`fixed inset-x-0 top-0 z-50 transition-all ${small?'py-2':'py-5'}`}><div className="container-av"><div className="flex items-center justify-between rounded-full border border-black/10 bg-white/85 px-4 py-3 backdrop-blur-xl"><a href="#top" className="focus-ring text-xl font-black tracking-tight">AVBOOK</a><nav className="hidden gap-8 text-sm uppercase tracking-[.18em] md:flex">{nav.map(n=><a className="hover:text-avred focus-ring" key={n.href} href={n.href}>{n.label}</a>)}</nav><a href="#contacts" className="focus-ring hidden rounded-full bg-black px-5 py-3 text-sm font-semibold text-white hover:bg-avred md:block">Получить каталог</a><button aria-label="Меню" onClick={()=>setOpen(!open)} className="focus-ring md:hidden">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="mt-2 rounded-3xl bg-white p-5 shadow-xl md:hidden">{nav.map(n=><a onClick={()=>setOpen(false)} className="block border-b py-3 text-lg" key={n.href} href={n.href}>{n.label}</a>)}<a className="mt-4 block rounded-full bg-black p-4 text-center text-white" href="#contacts">Получить каталог</a></nav>}</div></header>}
